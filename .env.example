DATABASE_URL="file:./dev.db"
APP_URL="http://localhost:3000"
SESSION_SECRET="replace-with-a-long-random-string"
APP_PASSWORD="changeme"

# Keep the SMTP password here.
# The rest of the email settings can now be saved inside the app Settings page.
SMTP_PASS=""

# Optional fallbacks if you want to define defaults in the environment.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_FROM=""
DEFAULT_REPORT_RECIPIENTS=""

GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
GOOGLE_REFRESH_TOKEN=""
EMAIL_PROVIDER="gmail_api"
